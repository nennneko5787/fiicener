name: fiicen
description: Fiicenerは、フィーセナーためのFiicenクライアントです。

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  html: ^0.15.4
  flutter_secure_storage: ^9.2.2
  image_picker: ^1.0.8
  flutter_linkify: ^6.0.0
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/fiicen.png
